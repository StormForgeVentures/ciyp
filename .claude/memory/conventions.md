# Project Memory — Conventions

> Conventions discovered or established during this project — "here, we do X."
> Project-specific by default; tag `[generalizable]` + `(role: <role>)` only when the
> convention should become a team-wide rule.

Format per entry:

### <convention> (role: <role>) [generalizable]?
**Convention:** the rule
**Applies when:** scope/trigger
**Source:** where it came from (human directive, discovered constraint, etc.)
**Date:** YYYY-MM-DD

---
