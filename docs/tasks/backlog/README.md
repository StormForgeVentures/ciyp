---
for_agent: |
  Lifecycle = location. Folders here are specced but NOT started. Promote by moving
  the whole prd-NNN-slug/ folder to ../in-work/ when its first wave begins; never
  edit status in frontmatter alone (there is no status frontmatter — location is
  the single source of truth). New PRD folders are created here first.
for_human: |
  Specced work that hasn't started. The plan gate reviews PRDs here before any
  wave picks them up.
---

# backlog/ — specced, not started
