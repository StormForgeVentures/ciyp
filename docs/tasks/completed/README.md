---
for_agent: |
  Lifecycle = location. Folders here passed the acceptance gate. Read-only for
  reference; never move a folder back out (a regression is NEW work — a new PRD
  folder in ../backlog/ citing this one). Never edit status in frontmatter alone —
  location is the truth. Completed ACs remain ledgered in handoff/acceptance-ledger.md.
for_human: |
  Accepted and done. Historical record — the acceptance ledger has the AC-by-AC
  trail.
---

# completed/ — accepted at the acceptance gate
