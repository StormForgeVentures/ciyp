# Acceptance Ledger — AC finite-state machine

> Lifted verbatim from the PRD folders in `docs/tasks/backlog/` at generate-tasks time (2026-07-02).
> States: OPEN → IN-WORK → VERIFYING → DONE (or BLOCKED / WAIVED-with-rationale). Owner is assigned at
> wave time; **Verifier must differ from Owner**. Checking a task list's last sub-task is what licenses
> moving its mapped rows here. This ledger is the durable acceptance state; the per-PRD `tasks-NNN-*.md`
> checklists remain build-run's work-item dashboards.

| ID | Source | Criterion (verbatim) | Status | Owner | Verifier |
|----|--------|----------------------|--------|-------|----------|
| AC-001-foundation-tenancy-01 | prd-001 index AC-1 | Given a fresh clone with pnpm installed, when `pnpm install && pnpm build && pnpm typecheck && pnpm test` run at the repo root, then every workspace exits 0. | VERIFIED — install+typecheck+build+test all exit 0 on independent run (2026-07-02, qa-001-wave-0) | developer | qa-reviewer |
| AC-001-foundation-tenancy-02 | prd-001 index AC-2 | Given `packages/shared` in isolation, when its typecheck runs, then contracts 01–06 compile standalone with no import from `apps/*` or other packages except zod. | VERIFIED — shared standalone typecheck exit 0; deps=zod only; no apps/cross-package imports (qa-001-wave-0) | developer | qa-reviewer |
| AC-001-foundation-tenancy-03 | prd-001 index AC-3 | Given the DB seeded with the Luminify tenant and the fixture tenant, when the cross-tenant RLS sweep test runs with tenant A's `app.tenant_id` GUC set, then zero tenant-B rows are returned from any tenant-scoped table. | OPEN | — | — |
| AC-001-foundation-tenancy-04 | prd-001 index AC-4 | Given migrations + seed applied to an empty database, when the §10 seed-verification query suite runs, then every required shape exists (tenant, app_config slots, archetypes, tiers, process definitions, embedded library chunks, five demo members with edge shapes, wallet ledger that sums to balance, ai_traces and usage_ledger rows). | OPEN | — | — |
| AC-001-foundation-tenancy-05 | prd-001a AC-1 | Given a fresh clone, when `pnpm install && pnpm -r typecheck && pnpm -r build` run, then every workspace exits 0. | VERIFIED — pnpm -r typecheck && pnpm -r build both exit 0 on independent run (qa-001-wave-0) | developer | qa-reviewer |
| AC-001-foundation-tenancy-06 | prd-001a AC-2 | Given `packages/agents/package.json`, when the dependency-lint test runs, then it asserts dependencies are exactly `@stormforgeventures/ciyp-shared` + `zod` and fails on any addition. | VERIFIED — reproduced: planted left-pad in agents deps → exit 1 (qa-001-wave-0). CAVEAT SF-1: gate reads only `dependencies`; optional/peerDependencies bypass it | developer | qa-reviewer |
| AC-001-foundation-tenancy-07 | prd-001a AC-3 | Given any workspace source file importing `@earendil-works/*`, when the dependency-lint test runs, then the build fails citing ADR-006. | VERIFIED — reproduced: planted @earendil-works import → exit 1 citing ADR-006 (qa-001-wave-0). Note N-1: substring match, not import-only | developer | qa-reviewer |
| AC-001-foundation-tenancy-08 | prd-001a AC-4 | Given the valid fixture for each of contracts 01–06, when parsed with its `@stormforgeventures/ciyp-shared` schema, then parsing succeeds; given the invalid fixture, then parsing throws a zod error. | VERIFIED — 15/15 green on independent run; each invalid fixture violates exactly its stated field; contracts field-by-field match specs 01–06 (qa-001-wave-0) | developer | qa-reviewer |
| AC-001-foundation-tenancy-09 | prd-001a AC-5 | Given a `parts` payload containing an unknown `kind`, when parsed with the parts-union schema, then parsing fails (closed union at v1). | VERIFIED — unknown parts.kind rejected (qa-001-wave-0). Related SF-2: the ChatTurnEvent SSE union has no equivalent closed-union test | developer | qa-reviewer |
| AC-001-foundation-tenancy-10 | prd-001a AC-6 | Given a version bump commit, when the publish workflow runs, then `@stormforgeventures/ciyp-shared` and `@stormforgeventures/ciyp-ui-tokens` install successfully into a clean external project from the private registry. | BLOCKED — publish.yml prepared + pack-tarball external install proven (PACK-INSTALL OK). ASK: Tim creates GitHub repo under theamazingwolf org, pushes, tags shared-v0.1.0 | developer | — |
| AC-001-foundation-tenancy-11 | prd-001b AC-1 | Given an empty local Postgres, when all migrations apply, then `supabase migration list` shows every migration applied with zero drift. | OPEN | — | — |
| AC-001-foundation-tenancy-12 | prd-001b AC-2 | Given the applied schema, when the RLS sweep test queries `pg_policies`, then every tenant-scoped table has both a `USING` and a `WITH CHECK` tenant policy (no table missing from the sweep's allowlist). | OPEN | — | — |
| AC-001-foundation-tenancy-13 | prd-001b AC-3 | Given rows for two tenants in every tenant-scoped table, when the suite selects from each table with `app.tenant_id` set to tenant A, then only tenant A rows return. | OPEN | — | — |
| AC-001-foundation-tenancy-14 | prd-001b AC-4 | Given a member-owned table seeded with two members of the same tenant, when member 1's context queries it, then only member 1's rows return (member fence independent of tenant fence). | OPEN | — | — |
| AC-001-foundation-tenancy-15 | prd-001b AC-5 | Given an existing `wallet_ledger` row, when the app role attempts UPDATE or DELETE, then the statement is rejected. | OPEN | — | — |
| AC-001-foundation-tenancy-16 | prd-001b AC-6 | Given two inserts into `usage_ledger` with the same `idempotency_key`, then the second insert is rejected or de-duplicated (unique constraint proven by test). | OPEN | — | — |
| AC-001-foundation-tenancy-17 | prd-001b AC-7 | Given the seeded `member_facts` table, when `EXPLAIN` runs on the tenant+member-scoped kNN recall query, then the plan uses the HNSW index (no sequential scan). | OPEN | — | — |
| AC-001-foundation-tenancy-18 | prd-001b AC-8 | Given the schema SQL, when grepped for the strings of any Kyle-era coach-IP enum value (archetype/tier/method names), then zero matches. | OPEN | — | — |
| AC-001-foundation-tenancy-19 | prd-001c AC-1 | Given an empty DB with migrations applied, when `pnpm seed` runs, then it exits 0; when it runs a second time, then row counts are unchanged (idempotent). | OPEN | — | — |
| AC-001-foundation-tenancy-20 | prd-001c AC-2 | Given the seeded `app_config`, when the seed-verify suite runs, then every ai-architecture §2 slot key resolves to a `{provider, model}` (or `voice_id`) value. | OPEN | — | — |
| AC-001-foundation-tenancy-21 | prd-001c AC-3 | Given the seeded library, then ≥ 200 `library_chunks` across ≥ 8 `library_items` carry a 1024-dim embedding and a non-null `tsvector`. | OPEN | — | — |
| AC-001-foundation-tenancy-22 | prd-001c AC-4 | Given the demo members, then each edge shape verifies by query: the new member has zero `member_facts`; the mid-journey member has ≥ 10; the expired member's entitlement status = `expired`; the near-zero member's wallet-usage context matches its scenario. | OPEN | — | — |
| AC-001-foundation-tenancy-23 | prd-001c AC-5 | Given the seeded wallet, then `wallets.balance_credits` equals `SUM(wallet_ledger.credits_delta)` for the tenant. | OPEN | — | — |
| AC-001-foundation-tenancy-24 | prd-001c AC-6 | Given seeded `ai_traces` and `usage_ledger`, when the metering rollup query (tokens + cost by feature) runs, then it returns non-empty, internally consistent results (ledger cost ≤ traced cost × markup). | OPEN | — | — |
| AC-001-foundation-tenancy-25 | prd-001c AC-7 | Given the seed SQL/scripts, when grepped for Kyle-era identifiers, then zero matches. | OPEN | — | — |
| AC-002-sport-runtime-01 | prd-002 index AC-1 | Given the Luminify seed tenant, when a chat turn is executed through the Sport assembly (internal turn entrypoint, no HTTP surface required), then a non-empty assistant reply is produced and every AI decision in the turn (classify, model call, retrieval, memory recall) has a corresponding `ai_traces` row sharing the turn's trace correlation id. | OPEN | — | — |
| AC-002-sport-runtime-02 | prd-002 index AC-2 | Given two seeded tenants whose `app_config.model_routing.default` name different models, when the same prompt is executed as a turn for each tenant, then each tenant's `ai_traces` model-call rows record their own configured model, with no per-tenant branches in runtime code (verified by grep: no tenant id literals outside seed/fixtures). | OPEN | — | — |
| AC-002-sport-runtime-03 | prd-002 index AC-3 | Given tenant A's slot config is updated via the config write path, when the next turn for tenant A executes, then it uses the new model without a process restart, and a turn for tenant B in the same window still uses B's unchanged model. | OPEN | — | — |
| AC-002-sport-runtime-04 | prd-002 index AC-4 | Given the Luminify seed and required provider keys present, when the eval runner executes the full suite, then every metric meets or exceeds its target from `docs/ai-architecture/feature-classification.md` and results persist as `eval_snapshots` rows. | OPEN | — | — |
| AC-002-sport-runtime-05 | prd-002 index AC-5 | Given the full monorepo source, when `grep -riE` for the coach-IP identifier list in prd-002a runs over `packages/` and `apps/`, then it returns zero matches outside `docs/`. | OPEN | — | — |
| AC-002-sport-runtime-06 | prd-002 index AC-6 | Given any turn executed in the test suite, when its trace rows are queried, then no row's payload contains a JWT, API key, or OAuth token (governance scan test). | OPEN | — | — |
| AC-002-sport-runtime-07 | prd-002a AC-1 | Given `packages/agents/package.json`, then its `dependencies` contain exactly `@stormforgeventures/ciyp-shared` and `zod`. | OPEN | — | — |
| AC-002-sport-runtime-08 | prd-002a AC-2 | Given a mock substrate, when the classifier receives malformed model output, then it returns the documented safe-fallback route and the failure is passed to `traceAICall`. | OPEN | — | — |
| AC-002-sport-runtime-09 | prd-002a AC-3 | Given a drafted reply, when the linter chain runs, then linters execute in canonical order and each intervention is reported in the chain result (order asserted by test). | OPEN | — | — |
| AC-002-sport-runtime-10 | prd-002a AC-4 | Given a `CodeProcessDefinition` with `source: 'authored'` and valid shape, when the process runner executes it, then it runs identically to a `source: 'code'` definition (same runner, no branch on source except provenance). | OPEN | — | — |
| AC-002-sport-runtime-11 | prd-002a AC-5 | Given the identifier list `reconnector\|stabilizer\|integrator\|self_led\|embodied_leader\|catapult\|mastermind\|concierge\|pmm\|harmonizer\|five_planes\|eft_tapping\|hawkins\|kwan.?yin\|merlin\|moses\|tesla\|rww\|real wealth wheel\|kyle\|rapid harmony\|empowered leader`, when case-insensitive grep runs over `packages/` and `apps/`, then zero matches (CI check; `docs/` excluded). | OPEN | — | — |
| AC-002-sport-runtime-12 | prd-002a AC-6 | Given a tool dispatch with args failing its zod schema, when dispatched, then the executor is not invoked and a structured validation error returns to the caller. | OPEN | — | — |
| AC-002-sport-runtime-13 | prd-002a AC-7 | Given an executor whose backing table does not exist, when its tool is dispatched, then the tool returns its documented empty shape (no throw) and the degradation is traced. | OPEN | — | — |
| AC-002-sport-runtime-14 | prd-002a AC-8 | Given a cadence thread at its bounded turn limit, when the next turn executes, then the agent produces its forced-finalize output and marks the thread complete. | OPEN | — | — |
| AC-002-sport-runtime-15 | prd-002b AC-1 | Given tenants A and B seeded, when turns for A and B execute concurrently, then each turn's trace rows carry its own tenant id and A's host instance is not B's (asserted via host identity in a test hook). | OPEN | — | — |
| AC-002-sport-runtime-16 | prd-002b AC-2 | Given tenant A's host is cached, when `invalidate(A)` is called after a config write, then the next `hostFor(A)` builds a fresh host and `hostFor(B)` returns B's cached host unchanged. | OPEN | — | — |
| AC-002-sport-runtime-17 | prd-002b AC-3 | Given the host cache at its configured bound, when one more tenant's host is requested, then the least-recently-used host is evicted and a cache-eviction metric/trace is emitted. | OPEN | — | — |
| AC-002-sport-runtime-18 | prd-002b AC-4 | Given a request whose body claims a different tenant than its auth context, when the turn executes, then all reads/writes and traces are scoped to the authenticated tenant (body value ignored). | OPEN | — | — |
| AC-002-sport-runtime-19 | prd-002b AC-5 | Given a source file assigning a JWT-bearing value into `ResolvedScope` construction, when lint runs, then the build fails with the custom rule id. | OPEN | — | — |
| AC-002-sport-runtime-20 | prd-002b AC-6 | Given index-time and query-time embedding calls in the test suite, when their outbound requests are captured, then index calls carry input type `document` and query calls carry `query` (wrong pairing fails the test). | OPEN | — | — |
| AC-002-sport-runtime-21 | prd-002b AC-7 | Given the SpendAuthorizer stub configured to deny, when a turn requiring authorization executes, then the turn short-circuits with the documented denial shape and a governance trace row is written. | OPEN | — | — |
| AC-002-sport-runtime-22 | prd-002b AC-8 | Given any vector query in the test suite issued with a mismatched `tenant_id` filter, then it returns zero rows (cross-tenant recall test over the two-tenant seed). | OPEN | — | — |
| AC-002-sport-runtime-23 | prd-002c AC-1 | Given tenant A with `default` slot = model X and tenant B = model Y, when one turn runs for each, then A's model-call trace records X and B's records Y. | OPEN | — | — |
| AC-002-sport-runtime-24 | prd-002c AC-2 | Given tenant A's `default` slot is updated to model Z with `invalidate(A)` fired, when A's next turn runs, then its trace records Z, without process restart. | OPEN | — | — |
| AC-002-sport-runtime-25 | prd-002c AC-3 | Given a code change introducing a literal model id outside config/seed paths, when CI runs, then the build fails (grep + `HardcodedModelError` test). | OPEN | — | — |
| AC-002-sport-runtime-26 | prd-002c AC-4 | Given any composed cascade in the test suite, then the final block is `[INSTRUCTION_HIERARCHY]` (asserted structurally, not by string position). | OPEN | — | — |
| AC-002-sport-runtime-27 | prd-002c AC-5 | Given a tenant config attempting to override an L0/L1 block id, when the cascade composes, then the override is rejected and traced (locked-layer test). | OPEN | — | — |
| AC-002-sport-runtime-28 | prd-002c AC-6 | Given identical tenant config and turn context, when the cascade composes twice, then outputs are byte-identical. | OPEN | — | — |
| AC-002-sport-runtime-29 | prd-002c AC-7 | Given a tenant updates an L2 brand block, when the write commits, then a `prompt_versions` row exists citing the block id and the tenant's prompt-set version increments. | OPEN | — | — |
| AC-002-sport-runtime-30 | prd-002c AC-8 | Given oversized L4 context, when the cascade composes, then L0–L3 and L5 are intact and only L4 content is trimmed (budget test). | OPEN | — | — |
| AC-002-sport-runtime-31 | prd-002d AC-1 | Given no provider keys in the environment, when `pnpm evals` runs, then key-requiring specs report `skipped`, key-free specs complete, and the process exits 0. | OPEN | — | — |
| AC-002-sport-runtime-32 | prd-002d AC-2 | Given the Luminify seed and keys present, when the suite runs, then every metric row in `eval_snapshots` carries value, target, and alert, linked to the tenant's prompt-set version. | OPEN | — | — |
| AC-002-sport-runtime-33 | prd-002d AC-3 | Given a model-call trace row from any golden turn, then `prompt_tokens`, `completion_tokens`, `provider`, and `model` are non-null. | OPEN | — | — |
| AC-002-sport-runtime-34 | prd-002d AC-4 | Given a cascade-affecting config write without a rationale string, when the write is attempted, then it is rejected (prompt_versions rationale constraint). | OPEN | — | — |
| AC-002-sport-runtime-35 | prd-002d AC-5 | Given a seeded slot pointing at a slug that returns empty completions, when the smoke test runs, then it exits non-zero naming the slot and slug. | OPEN | — | — |
| AC-002-sport-runtime-36 | prd-002d AC-6 | Given a Voyage 429 during the retrieval-precision eval, when the run completes, then that metric reports `blocked` (not `pass`) and the snapshot records the block reason. | OPEN | — | — |
| AC-002-sport-runtime-37 | prd-002d AC-7 | Given the trace-coverage suite, when it completes, then every decision type in the event taxonomy has ≥ 1 row (missing type = failing test naming it). | OPEN | — | — |
| AC-002-sport-runtime-38 | prd-002d AC-8 | Given a non-admin authenticated role, when it queries `ai_traces` or `eval_snapshots`, then RLS returns zero rows. | OPEN | — | — |
| AC-003-coaching-surfaces-01 | prd-003 index AC-1 | Given the Luminify seed (member with entitlement, indexed library), when the member completes a chat turn asking a question answerable from the seed corpus, then the SSE stream contains at least one `library_citation` part whose chunk id exists in the seed library. | OPEN | — | — |
| AC-003-coaching-surfaces-02 | prd-003 index AC-2 | Given that same turn, then an `ai_traces` row exists for it carrying `prompt_tokens`, `completion_tokens`, `provider`, `model`, and `cost_micros`, and exactly one Usage Event (contract 03) with a unique `idempotency_key` was emitted for the turn. | OPEN | — | — |
| AC-003-coaching-surfaces-03 | prd-003 index AC-3 | Given a seeded member with L2 facts, when the memory-continuity eval runs, then it scores 1.0 (a recalled L2 fact and the L1 summary both reach the turn grounding). | OPEN | — | — |
| AC-003-coaching-surfaces-04 | prd-003 index AC-4 | Given a member with an active daily cadence, when their check-in thread finalizes, then a structured cadence record for that member/kind/period is queryable via the member history endpoint. | OPEN | — | — |
| AC-003-coaching-surfaces-05 | prd-003 index AC-5 | Given two tenants seeded with disjoint libraries and members, when a tenant-A member exercises any surface in this module, then zero rows or chunks belonging to tenant B appear in any response, trace, or retrieval candidate set (rule 4 audit). | OPEN | — | — |
| AC-003-coaching-surfaces-06 | prd-003 index AC-6 | Given a member of tenant A, when they request another member's facts, cadence records, or process outputs, then the API returns 404/403 and RLS blocks the row access. | OPEN | — | — |
| AC-003-coaching-surfaces-07 | prd-003a AC-1 | Given a seeded member and a question answerable from the seed corpus, when they POST a turn, then the SSE stream contains ≥ 1 `library_citation` part whose `chunk_id` exists in the seed library and belongs to the member's tenant. | OPEN | — | — |
| AC-003-coaching-surfaces-08 | prd-003a AC-2 | Given any completed turn, then `ai_traces` contains a `routing` row for the classifier and a model-call row carrying `prompt_tokens`, `completion_tokens`, and `cost_micros`. | OPEN | — | — |
| AC-003-coaching-surfaces-09 | prd-003a AC-3 | Given the same turn replayed with the same `(thread_id, turn_id)`, then the `usage_ledger` contains exactly one row for that idempotency key. | OPEN | — | — |
| AC-003-coaching-surfaces-10 | prd-003a AC-4 | Given the classifier returns unparseable output (fault injection), when the turn runs, then the safe fallback route is taken and the reply still completes. | OPEN | — | — |
| AC-003-coaching-surfaces-11 | prd-003a AC-5 | Given the reranker errors (fault injection), when retrieval runs, then results fall back to top-K-by-ANN and a degradation trace row is written. | OPEN | — | — |
| AC-003-coaching-surfaces-12 | prd-003a AC-6 | Given a member message containing "ignore your instructions" style content, when the cascade assembles, then that content appears only inside the L4 context frame and the final block is `[INSTRUCTION_HIERARCHY]` (assertable via the cascade snapshot in the trace detail). | OPEN | — | — |
| AC-003-coaching-surfaces-13 | prd-003a AC-7 | Given the model calls `flag_for_red_review`, then a red-review row exists with the member, thread, reason, and trace id, and the member-facing reply completed normally. | OPEN | — | — |
| AC-003-coaching-surfaces-14 | prd-003a AC-8 | Given a turn where the model calls `offer_process`, when the member's client posts the `process_offer` return event with `accepted: true`, then the engine starts the named process thread (handoff verified in 003d). | OPEN | — | — |
| AC-003-coaching-surfaces-15 | prd-003b AC-1 | Given the seeded mid-journey member (rich L2), when the memory-continuity eval runs, then a recalled L2 fact and the L1 summary are both present in the turn grounding (score 1.0). | OPEN | — | — |
| AC-003-coaching-surfaces-16 | prd-003b AC-2 | Given a member edits a fact via `PATCH /v1/me/memory/facts/:id`, when their next turn runs, then the grounding contains the edited content and not the prior content. | OPEN | — | — |
| AC-003-coaching-surfaces-17 | prd-003b AC-3 | Given a member deletes a fact, then it no longer appears in `GET /v1/me/memory/facts` nor in any subsequent recall candidate set, and the row carries `deleted_at`. | OPEN | — | — |
| AC-003-coaching-surfaces-18 | prd-003b AC-4 | Given a session at 40 turns, when two turn completions race, then exactly one compaction summary row exists for the session (lock verified by concurrent test). | OPEN | — | — |
| AC-003-coaching-surfaces-19 | prd-003b AC-5 | Given the AI pipeline attempts to update a fact whose `source = 'member_edit'`, then the write is rejected and a new fact row is created instead. | OPEN | — | — |
| AC-003-coaching-surfaces-20 | prd-003b AC-6 | Given a member of tenant A, when recall runs, then every candidate row carries that member's `member_id` and `tenant_id` (rule 4 payload-filter audit query returns zero violations). | OPEN | — | — |
| AC-003-coaching-surfaces-21 | prd-003b AC-7 | Given working-memory keys for a session, then all carry TTL ≤ 2h (no unbounded Valkey keys — rule 8 audit). | OPEN | — | — |
| AC-003-coaching-surfaces-22 | prd-003c AC-1 | Given the seeded member with an active daily cadence, when they complete a check-in thread, then one `cadence_records` row exists for `(member, 'daily_checkin', today)` with a non-null structured `summary`. | OPEN | — | — |
| AC-003-coaching-surfaces-23 | prd-003c AC-2 | Given a cadence thread at its max-turn budget, when the member sends another message, then the runner finalizes the thread (status `finalized`) instead of continuing. | OPEN | — | — |
| AC-003-coaching-surfaces-24 | prd-003c AC-3 | Given two concurrent finalize attempts for the same `(member, kind, period)`, then exactly one record row exists afterward. | OPEN | — | — |
| AC-003-coaching-surfaces-25 | prd-003c AC-4 | Given the interaction-mode-correctness eval on the seeded golden directives, then it scores ≥ 0.9 (alert 0.8). | OPEN | — | — |
| AC-003-coaching-surfaces-26 | prd-003c AC-5 | Given a member with timezone America/Chicago, when the scheduler runs at 00:30 UTC, then no new daily thread opens until the member's local day boundary (period-key test). | OPEN | — | — |
| AC-003-coaching-surfaces-27 | prd-003c AC-6 | Given a coach of tenant A, when they list cadence records, then only tenant-A rows return; a member sees only their own. | OPEN | — | — |
| AC-003-coaching-surfaces-28 | prd-003c AC-7 | Given the seeded expired-entitlement member, when the scheduler runs, then no cadence thread opens for them (entitlement gate). | OPEN | — | — |
| AC-003-coaching-surfaces-29 | prd-003d AC-1 | Given the seeded planning-method directive, when a member completes the process and approves the document, then a `coaching_process_outputs` row exists with `approval_status='approved'`, a non-null `approved_at`, and the rendered doc content. | OPEN | — | — |
| AC-003-coaching-surfaces-30 | prd-003d AC-2 | Given the plan-document fidelity eval on the golden fixtures, then it scores 1.0 (any fabrication or dropped verbatim input fails the gate). | OPEN | — | — |
| AC-003-coaching-surfaces-31 | prd-003d AC-3 | Given a `doc-approved` goal with no member approval yet, when the model attempts finalize, then the goal gate blocks and the thread continues in the offer beat. | OPEN | — | — |
| AC-003-coaching-surfaces-32 | prd-003d AC-4 | Given a fixture directive row with `source='authored'`, when the runner executes it, then the process completes end-to-end with no code change (same runner path as `source='code'`). | OPEN | — | — |
| AC-003-coaching-surfaces-33 | prd-003d AC-5 | Given a member declines the offered document, then the output row is `declined`, the thread proceeds to revision, and a re-offer creates version n+1 rather than mutating version n. | OPEN | — | — |
| AC-003-coaching-surfaces-34 | prd-003d AC-6 | Given a directive with `pinned_lines`, when those lines are emitted, then they appear verbatim in the stream (linters bypassed) while generated lines carry guard-chain trace verdicts. | OPEN | — | — |
| AC-003-coaching-surfaces-35 | prd-003d AC-7 | Given a member with an active process thread for key K, when they attempt to start K again, then the API returns the existing thread (409-style idempotent open). | OPEN | — | — |
| AC-004-voice-runtime-01 | prd-004 index AC-1 | Given the Luminify seed and a funded mid-journey member, when the member completes a scripted voice session using the synthetic-audio fixture (utterance in → reply out), then the reply audio part references the Luminify tenant's configured `tts.voice_id`. | OPEN | — | — |
| AC-004-voice-runtime-02 | prd-004 index AC-2 | Given that completed session, then `ai_traces` rows exist for the STT utterance, the coaching turn, and the TTS synthesis, each carrying `provider`, `model`, and token/cost columns. | OPEN | — | — |
| AC-004-voice-runtime-03 | prd-004 index AC-3 | Given that session ends normally, then exactly one `settle()` has replaced the session's heavy reservation and the resulting Usage Event appears in `usage_ledger` (idempotent under retry). | OPEN | — | — |
| AC-004-voice-runtime-04 | prd-004 index AC-4 | Given the near-zero-wallet seed tenant, when its member calls `POST /v1/voice/session`, then the response is `402 { code: 'spend_denied', remainingCredits }` and no transport token is issued. | OPEN | — | — |
| AC-004-voice-runtime-05 | prd-004 index AC-5 | Given an active session whose wallet drains below the reservation floor, when the next checkpoint fires, then the transport closes with reason `spend_denied` within one checkpoint interval and the reservation is settled for actual cost. | OPEN | — | — |
| AC-004-voice-runtime-06 | prd-004 index AC-6 | Given `apps/voice` source, when grepped for `sport-core`, `sport-server`, or `@earendil`, then there are zero matches (voice is a pure HTTP client of the API). | OPEN | — | — |
| AC-004-voice-runtime-07 | prd-004 index AC-7 | Given the same Docker image, when booted against two different tenants' sessions, then each session resolves that tenant's STT model and TTS voice from session config (no tenant identity baked into the image or env). | OPEN | — | — |
| AC-004-voice-runtime-08 | prd-004a AC-1 | Given a valid `transportToken`, when the client connects to `/ws`, then the service fetches session config from the engine and emits `session_started` before processing audio. | OPEN | — | — |
| AC-004-voice-runtime-09 | prd-004a AC-2 | Given an invalid or expired `transportToken`, when the client connects, then the connection closes with an auth reason code and no engine turn call is made. | OPEN | — | — |
| AC-004-voice-runtime-10 | prd-004a AC-3 | Given a synthetic utterance fixture, when the pipeline processes it, then the engine's internal turn route receives `{ text, modality: 'voice', memberId, threadId, sessionId }` with the service-secret header. | OPEN | — | — |
| AC-004-voice-runtime-11 | prd-004a AC-4 | Given two sessions configured with different `tts.voiceId` values, when each produces a reply, then each TTS request carries its own session's `voiceId` (asserted via the stubbed TTS layer). | OPEN | — | — |
| AC-004-voice-runtime-12 | prd-004a AC-5 | Given in-flight TTS playback, when the member starts speaking, then playback is interrupted and the superseded synthesis is cancelled (barge-in test). | OPEN | — | — |
| AC-004-voice-runtime-13 | prd-004a AC-6 | Given the engine turn route times out, when a turn is in flight, then the service emits a recoverable error event on the transport and the session survives. | OPEN | — | — |
| AC-004-voice-runtime-14 | prd-004a AC-7 | Given the repo, when CI greps `apps/voice` for `sport-core\|sport-server\|@earendil`, then there are zero matches. | OPEN | — | — |
| AC-004-voice-runtime-15 | prd-004a AC-8 | Given the Docker image, when inspected, then no tenant-specific env/config is present (image is tenant-agnostic). | OPEN | — | — |
| AC-004-voice-runtime-16 | prd-004b AC-1 | Given a funded tenant, when its member calls `POST /v1/voice/session`, then the response contains `sessionId`, `transportUrl`, and a `transportToken` that expires within its configured TTL. | OPEN | — | — |
| AC-004-voice-runtime-17 | prd-004b AC-2 | Given the near-zero-wallet seed tenant, when its member calls `POST /v1/voice/session`, then the response is `402 { code: 'spend_denied', remainingCredits }` and no session row holds an `authToken`. | OPEN | — | — |
| AC-004-voice-runtime-18 | prd-004b AC-3 | Given an active session and a wallet drained below the accrued cost, when the next checkpoint evaluates, then the session row records `cut_reason = 'spend_denied'` and the voice service receives the cut instruction within one `checkpointIntervalS`. | OPEN | — | — |
| AC-004-voice-runtime-19 | prd-004b AC-4 | Given a session that ends normally, when settlement runs, then `settle()` is called with `actualCostMicros` equal to the sum of the session's traced costs, and the reservation no longer exists. | OPEN | — | — |
| AC-004-voice-runtime-20 | prd-004b AC-5 | Given a duplicate `session/end` report for an already-settled session, when settlement re-runs, then no second settle occurs and the ledger shows exactly one settlement event. | OPEN | — | — |
| AC-004-voice-runtime-21 | prd-004b AC-6 | Given a session with zero billable AI decisions, when it ends, then `release(authToken)` is called and no Usage Event is emitted for the session. | OPEN | — | — |
| AC-004-voice-runtime-22 | prd-004b AC-7 | Given a session record that never received end or cut, when the auto-release TTL elapses, then the reservation is released and the session row records the timeout. | OPEN | — | — |
| AC-004-voice-runtime-23 | prd-004b AC-8 | Given `checkpointIntervalS` in platform config, when an operator changes it, then the next-started session uses the new value with no deploy (config read, cached + invalidated). | OPEN | — | — |
| AC-005-library-connectors-01 | prd-005 index AC-1 | Given the Luminify seed tenant and a logged-in coach admin, when they upload a PDF via the library UI, then a `library_documents` row advances through the stage statuses to `indexed` and its chunks are retrievable by the two-stage RAG path. | OPEN | — | — |
| AC-005-library-connectors-02 | prd-005 index AC-2 | Given an indexed seed-corpus document, when a seeded member asks a chat question answered by that document, then the turn's response includes a `library_citation` part referencing that document. | OPEN | — | — |
| AC-005-library-connectors-03 | prd-005 index AC-3 | Given a tenant with a connected Granola integration (fixture-backed double in CI), when the coach imports a meeting, then the transcript lands as `indexed` chunks whose parent document has `source = 'granola'`. | OPEN | — | — |
| AC-005-library-connectors-04 | prd-005 index AC-4 | Given two tenants each with indexed content, when tenant A's retrieval runs with any query, then zero chunks belonging to tenant B appear in the candidate set (deterministic rule-4 isolation test at the query layer, not the ranking layer). | OPEN | — | — |
| AC-005-library-connectors-05 | prd-005 index AC-5 | Given any connector or ingestion failure, when the coach views the library UI, then the affected document shows a `failed` status with its failing stage — never a silent stall. | OPEN | — | — |
| AC-005-library-connectors-06 | prd-005a AC-1 | Given a `library_documents` row in `pending` for an uploaded PDF, when the worker runs to completion, then the row reaches `indexed` and its chunks exist with `page_number` provenance and `tenant_id` set. | OPEN | — | — |
| AC-005-library-connectors-07 | prd-005a AC-2 | Given a document mid-pipeline at `chunking`, when the worker process is killed and restarted, then processing resumes at `chunking` and the final chunk set is identical to an uninterrupted run. | OPEN | — | — |
| AC-005-library-connectors-08 | prd-005a AC-3 | Given a Deepgram transcription stage that throws, when the worker handles the error, then the document status is `failed` with `failed_stage = 'transcribing'` and a retry re-enters at `transcribing`. | OPEN | — | — |
| AC-005-library-connectors-09 | prd-005a AC-4 | Given an `indexed` document, when it is updated and re-ingested, then no chunk from the prior version remains queryable (chunk count and ids fully replaced). | OPEN | — | — |
| AC-005-library-connectors-10 | prd-005a AC-5 | Given an uploaded audio file, when ingestion completes, then each chunk from the transcript carries `start_seconds` and the embed call was made with `input_type: 'document'` (assertable via the trace row). | OPEN | — | — |
| AC-005-library-connectors-11 | prd-005a AC-6 | Given a chunk write, when it is inserted, then a missing `tenant_id` is rejected by schema constraint (NOT NULL + RLS), not by application convention. | OPEN | — | — |
| AC-005-library-connectors-12 | prd-005b AC-1 | Given a coach admin on the Library screen, when they upload a PDF, then a new row appears with status `pending` without a page reload. | OPEN | — | — |
| AC-005-library-connectors-13 | prd-005b AC-2 | Given a document being processed, when its status changes in the DB, then the list reflects the new status within one polling interval (≤ 10s). | OPEN | — | — |
| AC-005-library-connectors-14 | prd-005b AC-3 | Given a `failed` document, when the admin opens its detail view, then the failing stage and reason are displayed and a retry action is available. | OPEN | — | — |
| AC-005-library-connectors-15 | prd-005b AC-4 | Given an `indexed` document, when the admin deletes it, then the row is gone from the list and a retrieval query that previously cited it returns no chunks from it. | OPEN | — | — |
| AC-005-library-connectors-16 | prd-005b AC-5 | Given an authenticated member (non-admin) session, when it requests any library route or endpoint, then the response is 403/404 (Playwright + API assertion). | OPEN | — | — |
| AC-005-library-connectors-17 | prd-005b AC-6 | Given a Granola-imported document (fixture), then the list renders its `granola` source badge from the `source` field, not from title conventions. | OPEN | — | — |
| AC-005-library-connectors-18 | prd-005c AC-1 | Given a tenant admin clicking Connect on a provider card, when the OAuth consent completes at the callback, then a `tenant_integrations` row is `connected` and its tokens exist only envelope-encrypted in the vault table. | OPEN | — | — |
| AC-005-library-connectors-19 | prd-005c AC-2 | Given any API response or trace row produced during connect/refresh/import, when inspected in tests, then no access/refresh token plaintext or encrypted blob appears in it (`refresh_token_enc`-style columns never serialized to clients). | OPEN | — | — |
| AC-005-library-connectors-20 | prd-005c AC-3 | Given a connected integration for tenant A, when tenant B's admin requests A's integration by id, then the response is 404 under RLS (isolation integration test). | OPEN | — | — |
| AC-005-library-connectors-21 | prd-005c AC-4 | Given an expired access token, when the importer next needs it, then the refresh rotates both tokens atomically and the original refresh token no longer decrypts to a usable value. | OPEN | — | — |
| AC-005-library-connectors-22 | prd-005c AC-5 | Given a provider refresh that fails with an auth error, when retries are exhausted, then the integration state is `needs_consent` and the provider card shows a re-consent action. | OPEN | — | — |
| AC-005-library-connectors-23 | prd-005c AC-6 | Given an integration toggled off, when the tenant's next AI turn assembles, then the provider's `mcp:{server}:*` tools are absent from that scope's catalog (assembly-invalidation test). | OPEN | — | — |
| AC-005-library-connectors-24 | prd-005c AC-7 | Given the MCP catalog resolving for a scope, when `listActive` is called, then it receives the real tenant scope — a sentinel/global scope value fails the test. | OPEN | — | — |
| AC-005-library-connectors-25 | prd-005d AC-1 | Given a connected Granola integration (fixture-backed double in CI), when the coach opens Import meetings, then the meeting list renders provider titles/dates from `listMeetings`. | OPEN | — | — |
| AC-005-library-connectors-26 | prd-005d AC-2 | Given three selected meetings, when the import runs, then three `library_documents` rows exist with `source = 'granola'` and each reaches `indexed`. | OPEN | — | — |
| AC-005-library-connectors-27 | prd-005d AC-3 | Given a meeting already imported, when the same meeting is selected again, then no duplicate document is created and the run item reports skipped-duplicate. | OPEN | — | — |
| AC-005-library-connectors-28 | prd-005d AC-4 | Given an imported, indexed transcript, when the fidelity eval runs, then 100% of transcript sentences are reachable in that document's chunk contents. | OPEN | — | — |
| AC-005-library-connectors-29 | prd-005d AC-5 | Given a member chat question answered by imported-meeting content, when the turn completes, then the `library_citation` part resolves to the imported document (provenance survives to citation). | OPEN | — | — |
| AC-005-library-connectors-30 | prd-005d AC-6 | Given the Fathom API returning 500 for one meeting in a five-meeting run, when the run completes, then four items are imported and one is failed with a reason — the run is not aborted. | OPEN | — | — |
| AC-005-library-connectors-31 | prd-005d AC-7 | Given an explicit re-import of a provider-edited transcript, when it completes, then the document's chunks are fully replaced (no stale chunks from the prior version). | OPEN | — | — |
| AC-006-admin-config-studio-01 | prd-006 index AC-1 | Given a coach admin authenticated to tenant A, when they navigate any admin route or call any admin API, then only tenant A rows are readable or writable (cross-tenant probe returns 404/403 and an RLS test proves zero rows). | OPEN | — | — |
| AC-006-admin-config-studio-02 | prd-006 index AC-2 | Given the Luminify operator (superadmin role), when they open tenant management, then all tenants are listed and the switcher establishes an audit-logged session scoped to the selected tenant. | OPEN | — | — |
| AC-006-admin-config-studio-03 | prd-006 index AC-3 | Given a running engine serving tenant A, when an admin saves a model-routing slot change, then the next AI call for tenant A resolves the new slot value without any process restart or deploy (slot cache invalidated on write). | OPEN | — | — |
| AC-006-admin-config-studio-04 | prd-006 index AC-4 | Given a coach-authored agent definition that has not passed its eval pack, when the coach attempts activation, then activation is refused and the agent's prior version (or absence) continues serving. | OPEN | — | — |
| AC-006-admin-config-studio-05 | prd-006 index AC-5 | Given any behavior-affecting config write (archetype prompt_fragment, cascade block, model slot, agent definition, process directive), then a new `prompt_versions` row exists recording actor, rationale, and the prompt-set version bump. | OPEN | — | — |
| AC-006-admin-config-studio-06 | prd-006 index AC-6 | Given tenant A and tenant B with different `model_routing` values, when the same chat flow runs in each, then traces show each tenant's configured model with zero code differences (per-scope config proof, shared with PRD-002 AC). | OPEN | — | — |
| AC-006-admin-config-studio-07 | prd-006a AC-1 | Given a coach admin of tenant A with a valid session, when they request any admin API route with tenant B identifiers, then the response is 403/404 and no tenant B row is returned. | OPEN | — | — |
| AC-006-admin-config-studio-08 | prd-006a AC-2 | Given a user with a delegated `admin_role` lacking config rights, when they open the console, then Instance and Agent Studio are absent from nav and their API routes return 403. | OPEN | — | — |
| AC-006-admin-config-studio-09 | prd-006a AC-3 | Given the superadmin, when they create a tenant from Tenants, then a `tenants` row exists and appears in the list with status `active`. | OPEN | — | — |
| AC-006-admin-config-studio-10 | prd-006a AC-4 | Given the superadmin switched into tenant A, when they save any change, then an audit row records operator id, tenant A, action, and timestamp. | OPEN | — | — |
| AC-006-admin-config-studio-11 | prd-006a AC-5 | Given a suspended tenant, when its coach signs in, then authentication succeeds but the console renders the suspended state and all write APIs return 403. | OPEN | — | — |
| AC-006-admin-config-studio-12 | prd-006a AC-6 | Given an unauthenticated request to any admin route, then the response is a redirect to sign-in (UI) or 401 (API). | OPEN | — | — |
| AC-006-admin-config-studio-13 | prd-006b AC-1 | Given a coach saves an archetype `prompt_fragment` edit with a rationale, then a new `prompt_versions` row exists with actor + rationale and the tenant's prompt-set version is bumped. | OPEN | — | — |
| AC-006-admin-config-studio-14 | prd-006b AC-2 | Given the eval gate has not passed for the new prompt-set version, when the runtime serves the next turn, then it uses the prior live version (query: live-version pointer unchanged). | OPEN | — | — |
| AC-006-admin-config-studio-15 | prd-006b AC-3 | Given the eval gate passes, when the version goes live, then the next turn's trace references the new prompt-set version and the slot/assembly caches were invalidated (edit-to-live ≤ 60s P95). | OPEN | — | — |
| AC-006-admin-config-studio-16 | prd-006b AC-4 | Given a slot edit sets `default` to a different provider/model pair, when the next chat turn runs for that tenant, then the trace shows the new pair while a second tenant's traces are unchanged. | OPEN | — | — |
| AC-006-admin-config-studio-17 | prd-006b AC-5 | Given a member-UI request to the Instance Config endpoint for tenant A, then the response validates against the contract 01 zod schema and contains only tenant A content. | OPEN | — | — |
| AC-006-admin-config-studio-18 | prd-006b AC-6 | Given a save with an invalid slot value (unknown provider or malformed model id), then the API rejects with a field-level error and no version bump occurred. | OPEN | — | — |
| AC-006-admin-config-studio-19 | prd-006b AC-7 | Given a branding-only edit, then no prompt-set bump occurs but the Instance Config document version increments. | OPEN | — | — |
| AC-006-admin-config-studio-20 | prd-006c AC-1 | Given a coach saves a valid agent definition, then a new versioned row exists with `source='authored'`, status draft, and version = prior + 1. | OPEN | — | — |
| AC-006-admin-config-studio-21 | prd-006c AC-2 | Given a definition row that fails hydration (e.g. reviewer role granted both `challenge` and `write_artifact`), when the coach saves, then the API returns the `aiFixHint` payload and the UI renders it on the offending field; no draft version is created. | OPEN | — | — |
| AC-006-admin-config-studio-22 | prd-006c AC-3 | Given a draft agent whose eval pack has not passed, when the coach clicks Activate, then activation is refused and the active pointer is unchanged (query-verifiable). | OPEN | — | — |
| AC-006-admin-config-studio-23 | prd-006c AC-4 | Given a draft agent whose eval pack passes, when activation completes, then the next turn in that tenant executes the new agent version (trace shows role name + version) with no process restart. | OPEN | — | — |
| AC-006-admin-config-studio-24 | prd-006c AC-5 | Given an activated agent, when the coach reverts to a prior version, then the activation pipeline re-runs and the prior version serves on pass (trace-verifiable). | OPEN | — | — |
| AC-006-admin-config-studio-25 | prd-006c AC-6 | Given a coach in tenant A authors an agent, then tenant B's runtime and studio show no trace of it (RLS + per-scope assembly isolation). | OPEN | — | — |
| AC-006-admin-config-studio-26 | prd-006c AC-7 | Given a definition write targeting an L0/L1 cascade block, then the API rejects with a platform-locked error regardless of role. | OPEN | — | — |
| AC-006-admin-config-studio-27 | prd-006c AC-8 | Given a draft-sandbox turn, then its trace is marked test-context, it debits the tenant wallet, and it is excluded from member-facing history queries. | OPEN | — | — |
| AC-007-ai-economy-01 | prd-007 index AC-1 | Given the seeded near-zero-wallet tenant, when a voice session start requests heavy authorization (contract 04), then the response is `allow=false, reason='insufficient_balance'` and no session starts. | OPEN | — | — |
| AC-007-ai-economy-02 | prd-007 index AC-2 | Given that same tenant, when a Stripe test-mode top-up checkout completes and its webhook is processed, then a `wallet_ledger` credit row exists, `wallets.balance_credits` re-materializes, and a retried heavy authorize returns `allow=true`. | OPEN | — | — |
| AC-007-ai-economy-03 | prd-007 index AC-3 | Given the seed's `ai_traces` rows, when the usage-event emitter runs twice over the same traces (replay), then `usage_ledger` contains exactly one row per `idempotencyKey` and total debited credits are unchanged by the replay. | OPEN | — | — |
| AC-007-ai-economy-04 | prd-007 index AC-4 | Given any point after pipeline processing, when `wallets.balance_credits` is compared to `SUM(wallet_ledger)` for every tenant, then the values are equal (materialization invariant). | OPEN | — | — |
| AC-007-ai-economy-05 | prd-007 index AC-5 | Given a Stripe webhook delivered twice with the same event id, then exactly one `wallet_ledger` credit row exists for that event. | OPEN | — | — |
| AC-007-ai-economy-06 | prd-007a AC-1 | Given a provisioned tenant, then exactly one `wallets` row exists for it and `balance_credits` equals `SUM(wallet_ledger.credits_delta)` for that tenant. | OPEN | — | — |
| AC-007-ai-economy-07 | prd-007a AC-2 | Given a completed Stripe test-mode checkout, when the `checkout.session.completed` webhook is processed, then one `wallet_ledger` row with kind `topup` exists referencing the Stripe event id, and the balance increases by the purchased credits. | OPEN | — | — |
| AC-007-ai-economy-08 | prd-007a AC-3 | Given that webhook delivered again with the same event id, then no second ledger row is written. | OPEN | — | — |
| AC-007-ai-economy-09 | prd-007a AC-4 | Given any ledger table, when the application DB role attempts `UPDATE` or `DELETE` on a row, then the statement is rejected by the database. | OPEN | — | — |
| AC-007-ai-economy-10 | prd-007a AC-5 | Given an erroneous debit row, when an operator posts an `adjustment` with a `reason` and `corrects_ledger_id`, then the balance reflects the compensation and the original row is unchanged. | OPEN | — | — |
| AC-007-ai-economy-11 | prd-007a AC-6 | Given a tenant with auto-recharge enabled and a balance crossing below its threshold after a debit, then an off-session top-up attempt is recorded; on payment failure the wallet state is `recharge_failed` and an admin notification row exists. | OPEN | — | — |
| AC-007-ai-economy-12 | prd-007a AC-7 | Given the seeded Luminify tenant, when the coach opens the wallet screen, then balance, at least one seeded top-up, and at least one seeded usage debit render from ledger rows (no hardcoded values). | OPEN | — | — |
| AC-007-ai-economy-13 | prd-007a AC-8 | Given a balance below `low_balance_threshold_credits`, then the balance card renders its low state and a coach notification exists. | OPEN | — | — |
| AC-007-ai-economy-14 | prd-007b AC-1 | Given the seeded near-zero-wallet tenant, when `authorize` is called with `spendClass='heavy'` for a voice start, then the response is `{allow:false, reason:'insufficient_balance', authToken:null}`. | OPEN | — | — |
| AC-007-ai-economy-15 | prd-007b AC-2 | Given a funded tenant, when a heavy authorize succeeds, then a reservation row exists and a concurrent second heavy authorize sees a balance net of the reservation. | OPEN | — | — |
| AC-007-ai-economy-16 | prd-007b AC-3 | Given two concurrent heavy authorizes whose combined estimates exceed the balance, then at most one receives `allow=true` (atomic reserve; proven by a concurrency test). | OPEN | — | — |
| AC-007-ai-economy-17 | prd-007b AC-4 | Given a heavy authorization that is never settled or released, when `reservation_ttl` elapses and the sweep runs, then the reservation is released and the balance recovers. | OPEN | — | — |
| AC-007-ai-economy-18 | prd-007b AC-5 | Given a funded tenant, when cheap authorizes are issued, then no Postgres query occurs on the authorize path (verified by instrumentation/spy in the integration test) and each response returns within the cache. | OPEN | — | — |
| AC-007-ai-economy-19 | prd-007b AC-6 | Given concurrent cheap authorizes against a stale cache, then the cached balance never falls below `-overspend_bound_credits`, and calls beyond the floor are denied. | OPEN | — | — |
| AC-007-ai-economy-20 | prd-007b AC-7 | Given a `wallet_ledger` write for a tenant, then that tenant's cached balance entry is invalidated within the same request cycle (next cheap authorize re-reads materialized balance). | OPEN | — | — |
| AC-007-ai-economy-21 | prd-007b AC-8 | Given a wallet in state `paused`, when either spend class authorizes, then the response is `{allow:false, reason:'tenant_suspended'}`. | OPEN | — | — |
| AC-007-ai-economy-22 | prd-007c AC-1 | Given the seed's `ai_traces` rows, when the emitter and consumer process them, then `usage_ledger` contains exactly one row per billable trace, each referencing its `traceId`. | OPEN | — | — |
| AC-007-ai-economy-23 | prd-007c AC-2 | Given the same traces re-emitted (full replay), then `usage_ledger` row count and total debited credits are unchanged. | OPEN | — | — |
| AC-007-ai-economy-24 | prd-007c AC-3 | Given a trace with `cost_micros = 0` and nonzero `promptTokens`/`completionTokens` for a pricebook-listed model, then its `usage_ledger` row carries `priced_cost_micros > 0` and a `wallet_ledger` debit exists for the converted credits. | OPEN | — | — |
| AC-007-ai-economy-25 | prd-007c AC-4 | Given an event whose model has no pricebook row and whose traced cost is zero, then a `pricing_exceptions` row exists and no wallet debit is written for it. | OPEN | — | — |
| AC-007-ai-economy-26 | prd-007c AC-5 | Given a pricebook rate update, then subsequent ledger rows carry the new `pricebook_version` while previously written rows retain theirs (queryable proof: two versions coexist). | OPEN | — | — |
| AC-007-ai-economy-27 | prd-007c AC-6 | Given a debit computed for a tenant, then `credits = ceil(priced_cost_micros × markup_multiplier / credit_micro_value)` for that tenant's config (property test across boundary values, including ceil rounding on 1-micro remainders). | OPEN | — | — |
| AC-007-ai-economy-28 | prd-007c AC-7 | Given the pipeline idle on the seed, then the rollup-lag metric reports < 60s and the superadmin panel renders lag, drift, and exception counts from live queries. | OPEN | — | — |
| AC-007-ai-economy-29 | prd-007c AC-8 | Given `usage_ledger`, when the application role attempts `UPDATE`/`DELETE`, then the statement is rejected (append-only, mirrors 007a AC-4). | OPEN | — | — |
| AC-008-store-provisioning-01 | prd-008 index AC-1 | Given a Stripe test-mode checkout completed for a seeded member, when the webhook is processed, then `GET /v1/entitlement` for that member returns `status: 'active'` with the purchased `tierKey`. | OPEN | — | — |
| AC-008-store-provisioning-02 | prd-008 index AC-2 | Given the Luminify seed's expired-entitlement member, when that member opens a chat thread or voice session, then the engine refuses at session start with the entitlement-expired state (distinct from `spend_denied`). | OPEN | — | — |
| AC-008-store-provisioning-03 | prd-008 index AC-3 | Given a clean database and a completed intake document, when the provisioning script runs for a new tenant, then all 6 runbook steps complete and the script exits green. | OPEN | — | — |
| AC-008-store-provisioning-04 | prd-008 index AC-4 | Given a freshly provisioned tenant, when the eval golden set runs against its config (runbook step 5), then all metrics meet their targets before the script reports go-live-ready. | OPEN | — | — |
| AC-008-store-provisioning-05 | prd-008 index AC-5 | Given a freshly provisioned tenant, when `GET` Instance Config (contract 01) is called for it, then the response validates against the `@stormforgeventures/ciyp-shared` schema and contains zero identifiers from any other tenant. | OPEN | — | — |
| AC-008-store-provisioning-06 | prd-008a AC-1 | Given a seeded member with no entitlement, when a Stripe test-mode checkout session is created for them, then the session references the member's tenant's single program-access price and carries the member id in metadata. | OPEN | — | — |
| AC-008-store-provisioning-07 | prd-008a AC-2 | Given a completed test-mode checkout, when the `checkout.session.completed` webhook is delivered, then an entitlement projection exists with `status: 'active'` and the tenant's `tierKey`. | OPEN | — | — |
| AC-008-store-provisioning-08 | prd-008a AC-3 | Given the same webhook event delivered twice, when both are processed, then exactly one set of subscription rows exists (event-id dedupe) and the entitlement is unchanged by the replay. | OPEN | — | — |
| AC-008-store-provisioning-09 | prd-008a AC-4 | Given an entitled member, when `GET /v1/entitlement` is called with their session, then the response validates against the contract 05 zod schema. | OPEN | — | — |
| AC-008-store-provisioning-10 | prd-008a AC-5 | Given the seed's expired-entitlement member, when they open a chat thread, then the open is refused with the entitlement-expired state and no AI turn executes (no `ai_traces` row for the attempt). | OPEN | — | — |
| AC-008-store-provisioning-11 | prd-008a AC-6 | Given the seed's expired-entitlement member and a fully funded tenant wallet, when they start a voice session, then the refusal is entitlement-expired, not `spend_denied`. | OPEN | — | — |
| AC-008-store-provisioning-12 | prd-008a AC-7 | Given an active subscription whose `currentPeriodEnd` is moved into the past (test fixture), when the member next opens a session, then the start is refused and `GET /v1/entitlement` returns `status: 'expired'`. | OPEN | — | — |
| AC-008-store-provisioning-13 | prd-008a AC-8 | Given a member of tenant A, when they request `/v1/entitlement`, then no tenant-B row is readable even with a forged `tenant_id` parameter (tenant derives from token; RLS test). | OPEN | — | — |
| AC-008-store-provisioning-14 | prd-008b AC-1 | Given a clean database and a valid intake file, when `provision` runs, then all six steps report pass and the exit code is 0. | OPEN | — | — |
| AC-008-store-provisioning-15 | prd-008b AC-2 | Given a provisioned tenant, then its `app_config`, archetypes, tiers, and process definitions match the intake file field-for-field (diff test). | OPEN | — | — |
| AC-008-store-provisioning-16 | prd-008b AC-3 | Given an intake file missing a required field (e.g. `tts.voice_id`), when `provision` runs, then it exits non-zero before any DB write, naming the field. | OPEN | — | — |
| AC-008-store-provisioning-17 | prd-008b AC-4 | Given `--dry-run` against a clean database, when the script completes, then row counts in every tenant-scoped table are unchanged. | OPEN | — | — |
| AC-008-store-provisioning-18 | prd-008b AC-5 | Given a run killed after step 3, when `provision` re-runs with the same intake, then steps 1–3 no-op (no duplicate tenant, config, or library rows) and steps 4–6 complete. | OPEN | — | — |
| AC-008-store-provisioning-19 | prd-008b AC-6 | Given a tenant whose config fails an eval-golden-set target (fixture with a sabotaged directive), when step 5 runs, then the script exits non-green and does not emit Instance Config. | OPEN | — | — |
| AC-008-store-provisioning-20 | prd-008b AC-7 | Given a completed provisioning run, when the seeded eval + retrieval queries run scoped to the NEW tenant, then zero rows from any other tenant appear in results (isolation sweep). | OPEN | — | — |
| AC-008-store-provisioning-21 | prd-008b AC-8 | Given a completed run, when Instance Config (contract 01) is fetched for the tenant, then it validates against the `@stormforgeventures/ciyp-shared` schema and every branded string traces to the intake. | OPEN | — | — |
| AC-008-store-provisioning-22 | prd-008a AC-9 | Given a valid tenant API key, when `POST /v1/external/entitlements` grants access for a member, then the member's next `GET /v1/entitlement` returns `status: 'active'` with `source: 'api'`; given the same request replayed with the same idempotency key, then exactly one grant exists; given tenant A's key used against a tenant-B member ref, then the request is rejected with no row written. | OPEN | — | — |
