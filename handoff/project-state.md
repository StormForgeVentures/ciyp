# Project State — append-only decisions log

> Updated at wave boundaries by the PM (or orchestrator). Every agent reads this at startup
> instead of replaying upstream artifacts. Binding decisions only — keep each entry ≤ 3 lines.
> Never delete entries; supersede them ("supersedes #N").

## Current constraints (live summary — PM maintains, ≤ 20 lines)

- **PIPELINE:** discovery ✓ · ai-design ✓ · architecture ✓ · prd ✓ · generate-tasks ✓ · waves ✓ · **plan gate ✓ (Tim, 2026-07-02 — "we are a go")** · **BUILD ← ACTIVE** · acceptance ☐
- **NEXT (resume here):** **wave 2 ✓ MERGED to main + CI green** (bdac332). Three tracks integrated: 002 §2-4 Sport runtime ∥ 006 §1.0 admin shell ∥ 008 §1.1-1.3 store. Integrated tree: 9/9 packages, api 113, db isolation 11, seed-verify pass, purity clean. Boundary QA+Security: **not merge-blocking** (Security 0 Crit/1 High/2 Med; H2 + context-forgery **CLOSED** live on all reachable surfaces). **CARRY-FORWARD (must-do before the relevant surface is exposed):** (1) **002 §4.0 eval harness is PARTIAL — 2 of 8 metrics real** (`registry.keyGated()` returns null; no judge.ts/golden fixtures); "no-eval-no-ship" is ceremony for 6/8 → **002 §4.0 NOT done, module AC-4 PARTIAL, eval AC rows withheld**. Complete before 002 acceptance. (2) **H-1: store interim HS256 auth (`store/auth.ts`)** = weaker disjoint trust-root on money/PII routes; not exploitable at rest (SESSION_JWT_SECRET unset, no minting route) but **replace with Supabase-Auth member verification before member traffic**. (3) **M-1: fail-loud boot validation** for SESSION_JWT_SECRET + CONNECTOR_VAULT_KEY. (4) **three-auth reconciliation** → PRD-003 (no correctness conflict; SET LOCAL on separate pools + disjoint path prefixes proven safe). (5) **admin browser suite has ZERO CI coverage** (skipIf !GoTrue + excluded from pnpm test) — CI needs a Supabase/GoTrue stack or the spine stays locally-tested only. **Next build: wave 3** (003 coaching-core ∥ 005 ingestion ∥ 004 voice ∥ 007 wallet) — but 003 owns the PRD-003 auth-reconciliation + real member HTTP route (M-2/H-1). Design review gate w/ Tim still open (Figma IS populated — see below).
- **wave-1 note:** wave 1 ✓ COMPLETE + merged (1410fe4). Both tracks passed the wave-boundary gate: engine port (002 §1.0, ac7b120) QA merge-ready 0 must-fix; schema+seed (001 §3.0–4.0) passed QA+Security after a fix loop that closed the ledger-TRUNCATE Critical (C1) + member-fence fail-open (H1). 25 ledger rows VERIFIED (AC-…-17 HNSW WITHHELD → architect wording call). Merged main green: seed-verify 41/41, isolation 11/11, 238 unit tests, purity clean. **NEXT: wave 2** (002 §2.0–4.0 Sport assembly/ports/slots/evals ∥ 006 §1.0 admin shell ∥ 008 §1.1–1.3 store) — but **design review gate with Tim is open first** (4 Qs) before wave-2 UI. `design-app` output is in the CIYP Figma file.
- **Wave-2 carry-forward constraints (from wave-1 review):** (a) **H2** — apps/api MUST set `app.tenant_id`/`app.member_id`/`app.context` transaction-locally from the *verified* session only (GUCs are asserted-not-JWT-bound; a member forging `app.context='coach'` sees all members — proven); add spoofed-param invariant test + lock PostgREST anon/authenticated surface. (b) **L2** — `memberDocCues` regex (data seeded in `app_config.engine_config`) needs a runtime bound (length cap / flag allowlist / re2-or-timeout) in the engine/apps-api layer before it compiles tenant-authored patterns. (c) **AC-7 HNSW** wording → architect. (d) **voyage-3-large vs §2/ADR-007 voyage-3.5** → ratify seed value or plan re-embed (renaming invalidates cached fixtures + costs Voyage spend).
- PRD output: 9 PRD folders (34 spec docs; PRD-009 = index-locked backlog module) + 8 task lists + `handoff/acceptance-ledger.md` (230 AC rows, all OPEN) + `handoff/reuse-map.md` (donor-code map). All plan-gate decisions resolved (#13–#15); ADR-008 fixes the money topology (coach-Stripe connector, GHL-style).
- ai-design outputs: `docs/ai-architecture/feature-classification.md` (18 features: 2 AI-native · 9 Hybrid · 7 Traditional) + `docs/ai-architecture/ai-architecture.md` (slots) + ADR-006 (Sport runtime) + ADR-007 (EL-OS inheritances, closes OQ-1). Orientation input: `handoff/sport-sdk-orientation.md`.
- Discovery output: `docs/project-brief.md` (canonical system brief). Architecture: `docs/architecture.md` + `docs/adr/` (7) + `docs/contracts/` (6).
- Sibling repo `ciyp-template` = thin member UI (MVP); blocked until the 6 contracts are frozen here.
- SDK dependency discipline: cite sport-ai-sdk issue numbers (#25–#27 critical path for coach-authored agents; #28–#31 mid-build), NEVER a version pin (Tim 2026-07-02).
- Open for Tim: OQ-2 only (Luminify archetypes/tiers content — provisioning input; seed uses placeholders). OQ-3 resolved: markup default 1.1× (decision #13).
- **Figma (Tim, 2026-07-02):** CIYP target file `https://www.figma.com/design/BXlkljEYi55XOEa5DX5kjw/CIYP` is **POPULATED** (design-app run DID land — 788 nodes: token system light/dark, component state matrices, admin-shell + wallet screens full-depth, library/connectors/instance/studio at composition depth; corrects the earlier "empty" report, which was a stale top-level page listing — node 4:4 etc. are all present). EL-OS donor reference: `https://www.figma.com/design/uLPSJirBSuvUDFNjDsiD8G/ELOS---Rapid-Harmony`. Design review gate w/ Tim still open (4 Qs: font / accent default / empty guidelines page / density — all token-swappable). **Stripe test key: PINNED for later (Tim) — store webhook/checkout mock-verified; live-test AC-1/2 pending the key.**

## Decisions log

| # | Date | Wave | Role | Decision (binding for downstream) |
|---|------|------|------|-----------------------------------|
| 1 | 2026-06-18 | discovery | PM | Two repos, engine-first: `ciyp-platform` (engine+platform, production) built before `ciyp-template` (thin member UI, MVP). |
| 2 | 2026-06-18 | discovery | PM | Tenancy = shared multi-tenant now, promotable to dedicated per-coach engine+DB later (ADR-001). Coach = tenant. |
| 3 | 2026-06-18 | discovery | PM | Three money flows: member→coach (Stripe web checkout, no IAP); coach→Luminify (prepaid AI wallet, metered, hard enforcement in v1); coach absorbs members' AI cost. |
| 4 | 2026-06-18 | discovery | PM | Seed coach = Luminify (Tim's own coaching business). Instance-agnostic: no Kyle content in either repo. |
| 5 | 2026-06-18 | discovery | PM | Voice = P0 (native-first). De-enum surface = archetype/tier/coaching-method → per-tenant config. |
| 6 | 2026-06-18 | architecture | software-architect | Architecture drafted (extraction of EL-OS). NOTE: produced before ai-design — AI-stack/model-slots must be ratified by the AI-architect in the ai-design phase. |
| 7 | 2026-07-02 | ai-design | ai-architect | Sport AI SDK is the agent runtime (ADR-006). Pure brain stays pure; per-scope assembly + live slot config mandatory (ScalingCFO anti-patterns prohibited). SDK↔platform boundary per `handoff/sport-sdk-orientation.md` §6; gaps ship as SDK issues. |
| 8 | 2026-07-02 | ai-design | ai-architect | AI stack ratified: Sport slot taxonomy stored per-tenant (`app_config.model_routing`), OpenRouter gateway, Claude-family default/fast, Voyage embed+rerank, Deepgram streaming STT, Fish-audio per-tenant TTS (ADR-007, closes OQ-1). Paid Voyage key = build prerequisite. |
| 9 | 2026-07-02 | ai-design | ai-architect | Feature lanes locked (`docs/ai-architecture/feature-classification.md`): 2 AI-native, 9 Hybrid, 7 Traditional. Coach-authored agent studio = Hybrid core surface; depends on sport-ai-sdk issues #25–#27 (cite issues, never versions). Off-table model metering priced by platform pricebook, not SDK derivedCost (OQ-A → metering PRD). |
| 10 | 2026-07-02 | ai-design | Tim | Granola/Fathom meeting-recording import = **P0** in v1 (supersedes brief's P1). Connector layer (per-tenant MCP catalog + OAuth vault) enters v1 scope; reuses library ingestion pipeline. |
| 11 | 2026-07-02 | ai-design | Tim | `apps/web` = coach/admin only (centralized). Member web/desktop = the template's **PWA** (responsive incl. desktop), per-client optional — no member area in apps/web, no extra member surface. |
| 12 | 2026-07-02 | prd | PM | v1 speced as 8 PRD folders (foundation · sport-runtime · coaching-surfaces · voice · library+connectors · admin+studio · ai-economy · store+provisioning), 229 ledgered ACs, 6-wave plan. Ledger-side pricebook pricing (contract 03 unchanged) proposed pending plan-gate ratification. 5 open decisions parked as wave-plan checkboxes for Tim. |
| 13 | 2026-07-02 | plan gate | Tim | Pricing resolved: pricebook ratified (default rate rule + per-model overrides; per-tenant markup, default **1.1×**); voice cut = finish in-flight reply then close (balance may go bounded-negative); auto top-up confirmed (= 007a auto-recharge); storage costs excluded from usage billing (tenant subscription/maintenance). |
| 14 | 2026-07-02 | plan gate | Tim | Store: subscription = built-in default; flat-fee/free grants supported; **external enrollment API added (008a FR-9 / AC-22)** for GHL-style workflows. **Coach→member token-cost passthrough PARKED as post-v1 PRD** (contradicts v1 "members never see credits"; schema must not preclude — rates are stacked config). Remaining ledger total: 230 ACs. |
| 15 | 2026-07-02 | plan gate | Tim | **ADR-008 money topology (supersedes the parking in #14, refines #3):** member payments settle on the COACH's own Stripe — platform never holds coach/member money; only flow (b) touches our account. **Mechanism: GHL-style direct integration (coach supplies a restricted API key; we create products/webhooks on their account) via the 005c connector vault — Stripe Connect considered and NOT chosen for v1** (swappable behind the connector port). Member credit billing = per-tenant `member_billing_mode` (`absorbed` default \| `member_credits`), coach's choice, baked in now: v1 builds the coach-Stripe connector + mode switch + non-preclusion; **PRD-009** (backlog) builds the member-credits economy. 001b/008a/008b updated. |
| 16 | 2026-07-02 | plan gate | Tim | **PLAN GATE APPROVED** ("Seems like we are a go"). Build begins at wave 0 (prd-001). Repo git-initialized with the approved plan as the baseline commit. |
| 17 | 2026-07-02 | wave 0 | PM | Repo home = **`StormForgeVentures/ciyp`** (Tim's new org — supersedes the theamazingwolf assumption). Published packages renamed **`@stormforgeventures/ciyp-shared` / `@stormforgeventures/ciyp-ui-tokens`** (GitHub Packages requires scope = repo owner); internal-only packages stay `@ciyp/*`; sport-ai-sdk installs remain `@theamazingwolf/*` (separate org token). Local Supabase runs on **553xx ports** (coexists with EL-OS defaults). |
| 18 | 2026-07-02 | wave 1 | Tim+PM | **Seed content domain (001c):** primary corpus = Luminify **AI-adoption/AI-coding coaching** (authentic to seed tenant; factual → checkable RAG/citation evals) + **one journaling/reflection archetype + process directive** (Mind Allah riff) to exercise memory extraction + cadence. OQ-2 unchanged: real archetype/tier copy authored by Tim at provisioning. Markup in seed = **1.1×** per decision #13 (supersedes the stale 1.5× note in task 4.3). |
| 21 | 2026-07-02 | product direction | Tim | **Authored guardrails = THE WAY TO GO** (captured as binding direction; **ai-feature-design pass DEFERRED — Tim: "don't run pass on it yet"**). Agents AND their inline guardrails must be **defined in the SaaS UI, not code**. The per-turn linter layer (today the fixed voice/no_shame/playfulness/retention chain in `packages/agents`) becomes **per-agent authored config**: (1) a platform-mandatory **floor** (safety/harm — non-removable), (2) coach-**selectable/removable** guardrails from a catalog, (3) coach-**authored checks** — natural-language requirement → AI-generated inline check (fast-slot judge or deterministic rule) that runs linter-style (block/repair on live output, in the loop). This is DISTINCT from the **offline eval harness** (002 §4.0), which Tim is happy with as-is = metrics/observability + go-live gate; NEW sub-idea worth building: **track eval-surfaced improvement opportunities as a backlog**. Reshapes **Agent Studio (006c)** + needs an ai-feature-design pass (deferred). **SCOPING OPEN:** authored guardrails in v1 (wave 5) vs designed-in target for v1.1 — architecture must not preclude either. |
| 20 | 2026-07-02 | wave 1 | PM/architect | **Two wave-1 review reconciliations:** (a) **Seed embed model = `voyage-3-large`** @1024-dim (ratified as-is; index+query embedder must be the SAME model — cross-model vectors don't match; fixtures cached; provisioning may move to `voyage-3.5` but re-embeds doc+query together, HNSW/schema unaffected). ai-architecture §2 note updated. (b) **AC-001-…-17 (HNSW) reworded** to "HNSW is a usable access path (exact-scan alternatives forced off)" — literal "no seq scan under default planner" is unsatisfiable at seed volume (optimizer correctly exact-scans ~31 rows); natural selection is a prod-scale property (architecture §4.4). prd-001b AC-7 + ledger updated → row now DONE. |
| 19 | 2026-07-02 | wave 1 | PM+security | **RLS tenancy is GUC-asserted, not JWT-bound (binding for apps/api wave):** DB enforces `app.tenant_id`/`app.member_id`/`app.context` via GUC readers; there is NO DB-layer defense against a backend that sets the wrong value. apps/api MUST set all three transaction-locally from the *verified* session only, never forward client-supplied values, and carry an invariant test that a member principal can never yield `context='coach'`. Member fence is fail-closed (unset → 0 rows). H2 open at DB layer BY DESIGN — closed only in apps/api. |
