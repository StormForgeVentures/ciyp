{
  "name": "@ciyp/agents",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "THE BRAIN \u2014 pure, provider-agnostic AI agents (orchestrator, classifier, linters, coaching/cadence agents, interaction engine). Deps are EXACTLY @ciyp/shared + zod (enforced by dependency-lint). Populated by PRD-002a; scaffold in PRD-001a.",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint ."
  },
  "dependencies": {
    "@ciyp/shared": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@eslint/js": "^9.16.0",
    "eslint": "^9.16.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.17.0",
    "vitest": "^2.1.8"
  }
}
