/**
 * @ciyp/prompts — versioned prompt content + composition machinery.
 * Scaffold only (PRD-001a); the EL-OS port lands in PRD-002a. ZERO runtime dependencies
 * (pure content/composition — enforced by scripts/dependency-lint.mjs). Zero coach IP:
 * platform-generic content only; tenant content lives in per-tenant config rows (ADR-002).
 */
export const PROMPTS_SCAFFOLD_VERSION = '0.0.0';
